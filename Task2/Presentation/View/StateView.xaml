<UserControl x:Class="Presentation.View.StateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate x:Key="RowButtons">
            <StackPanel Orientation="Horizontal">
                <Button Content="Edit"/>
                <Button Content="Delete"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <StackPanel DataContext="{Binding StateViewModel, Source={StaticResource Locator}}">
        <StackPanel Orientation="Horizontal">
            <Button Content="Add a State" x:Name="AddStateBtn" Command="{Binding AddStateCommand}" Width="200" Height="50" Margin="10 10 10 10"/>
            <Button Content="Refresh States" x:Name="RefreshStateBtn" Command="{Binding RefreshStateCommand}" Width="200" Height="50" Margin="10 10 10 10"/>
        </StackPanel>
        <ListView ItemsSource="{Binding StatesList}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="200"/>
                    <GridViewColumn Header="Is it borrowed?" DisplayMemberBinding="{Binding IsBorrowed}" Width="200"/>
                    <GridViewColumn CellTemplate="{StaticResource RowButtons}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</UserControl>
